main.py garbage functions
def check_ghost_wall_collisions(walls, ghost):
    for wall in walls:
        if pygame.sprite.collide_rect(wall, ghost):
            return True
    return False


def min_distance_to_move(direction, walls, player):
    if direction=='U':
        min = player.speed
        for wall in walls:
            if wall.y+wall.height<=player.rect.y+10 and wall.x<player.rect.centerx<wall.width:
                if player.rect.y-(wall.y+wall.height)<min:
                    min = player.rect.y-(wall.y+wall.height)
        return min
    elif direction=='D':
        min = player.speed
        for wall in walls:
            if wall.y>=player.rect.y+player.rect.height-10 and wall.x<player.rect.centerx<wall.width:
                if wall.y-(player.rect.y+player.rect.height)<min:
                    min = wall.y-(player.rect.y+player.rect.height)
        return min
    elif direction=='L':
        min = player.speed
        for wall in walls:
            if wall.x+wall.width<=player.rect.x+10 and wall.y<player.rect.centery<wall.height:
                if player.rect.x-(wall.x+wall.width)<min:
                    min = player.rect.x-(wall.x+wall.width)
        return min
    elif direction=='R':
        min = player.speed
        for wall in walls:
            if wall.x>=player.rect.x+player.rect.width-10 and wall.y<player.rect.centery<wall.height:
                if wall.x-(player.rect.x+player.rect.width)<min:
                    min = wall.x-(player.rect.x+player.rect.width)
        return min